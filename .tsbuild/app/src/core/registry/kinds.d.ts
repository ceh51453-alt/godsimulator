/**
 * Mười bốn kind dựng sẵn — Phần 4.3 [MR].
 *
 * [BB] `phanChieu` là DỮ LIỆU. Quy tắc chiếu (Phần 18.2) đọc từ đây, không hardcode.
 * Người dùng thêm loại thực thể mới bằng JSON, không sửa code.
 */
import type { KindDef } from './types.js';
export declare const KINDS_DUNG_SAN: readonly KindDef[];
export declare const KIND_IDS: string[];
/** Mười bốn kind của Phần 4.3 — cổng Phase 0 kiểm đúng danh sách này. */
export declare const KIND_IDS_43: string[];
/** Kind nền của Phase 5. */
export declare const KIND_IDS_NEN: string[];
/** Kind tầng Thần của Phase 6. */
export declare const KIND_IDS_THAN: string[];
